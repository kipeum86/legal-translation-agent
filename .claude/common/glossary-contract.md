# Glossary Contract Capsule

Glossary hierarchy is Library custom > persistent glossary > LLM proposal. Locked glossary terms must be used exactly whenever the source term appears.

Use deterministic term candidates before LLM refinement, then run `check-glossary-usage.py` before delivery. Missing locked target terms are blocking failures.
