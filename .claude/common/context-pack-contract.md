# Context Pack Contract Capsule

Use `.claude/scripts/build-context-pack.py` to create segment-level context instead of injecting full source, full glossary, and full language guides into every pass.

Use `.claude/scripts/estimate-context-cost.py` to compare before/after estimated input tokens. Completion is measured by a report, not a fixed percentage target.
